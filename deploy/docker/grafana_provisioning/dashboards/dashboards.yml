apiVersion: 1

providers:
  - name: dalikamata
    folder: Dalikamata
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
  - name: archive
    folder: Archive
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards-archive